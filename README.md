# sol2-recipe
Sol2 Conan Recipe for RageDB

This recipe is compatible with Conan 2.x.

## Usage

To build, package, and test the library locally, run:

```bash
conan create . --version 3.3.0-luajit
conan create . --version 3.3.0-exhaustive
conan create . --version 3.3.1-exhaustive
```